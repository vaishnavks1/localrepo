## 2025-07-15 - HTML Boilerplate Optimization
**Learning:** Even a minimal static HTML file can be optimized for performance. Switching to Standards Mode and specifying the charset early reduces the browser's work during initial parse.
**Action:** Always ensure `<!DOCTYPE html>` and `<meta charset="UTF-8">` are present in static projects.
